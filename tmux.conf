set -g default-terminal "screen-256color"
set -s escape-time 0

unbind r
bind r source-file ~/.tmux.conf \; display "Reloaded."

# bind ; 

set -g base-index 1

unbind C-Space
set -g prefix C-Space
bind C-Space send-prefix

setw -g mode-keys vi
bind-key h select-pane -L
bind-key j select-pane -D
bind-key k select-pane -U
bind-key l select-pane -R

bind-key m resize-pane -Z

bind-key J source-file "~/.config/tmux/macros/maximize-down.conf"
bind-key K source-file "~/.config/tmux/macros/maximize-up.conf"
bind-key H source-file "~/.config/tmux/macros/maximize-left.conf"
bind-key L source-file "~/.config/tmux/macros/maximize-right.conf"

set -g renumber-windows on

bind - split-window -v
bind \\ split-window -h

set -g mouse on

# ~~~~~~ COLORSCHEME ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ 
set -g @plugin "o0th/tmux-nova"

set -g @nova-segment-session "#{session_name}"
set -g @nova-segment-session-colors "#3a3f4c #ddeeff"

set -g @nova-pane "#I#{?pane_in_mode,  #{pane_mode},}  #W"

set -g @nova-status-style-bg "#19191e"
set -g @nova-status-style-fg "#ddeeff"
set -g @nova-status-style-active-bg "#3a3f4c"
set -g @nova-status-style-active-fg "#ddeeff"

set -g @nova-rows 0
set -g @nova-segments-0-left ""
set -g @nova-segments-0-right "session"
# ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

set -g @plugin "tmux-plugins/tpm"
set -g @plugin "tmux-plugins/tmux-sensible"

run "~/.config/tmux/plugins/tpm/tpm"
