- defaults:
    link:
      relink: true

- clean: ['~']

#- link:
#    ~/.tmux.conf: tmux.conf
#    ~/.vim: vim
#    ~/.vimrc: vimrc

- create:
   - ~/Work
   - ~/test/nested/directories

#- shell:
#  - [git submodule update --init --recursive, Installing submodules]
